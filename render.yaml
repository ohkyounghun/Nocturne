services:
  - type: web
    name: nocturne
    runtime: node
    buildCommand: npm install && npm run seed
    startCommand: npm start
    envVars:
      - key: PORT
        value: 3000
      - key: JWT_SECRET
        sync: false
      - key: KAKAO_API_KEY
        sync: false
